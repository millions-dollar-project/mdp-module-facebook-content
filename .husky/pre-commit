#!/usr/bin/env sh
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-commit: typecheck (plugin)"
pnpm typecheck
echo "pre-commit: test (plugin + backend)"
pnpm test
echo "pre-commit: secret scan"
exec bash scripts/secret-scan.sh
